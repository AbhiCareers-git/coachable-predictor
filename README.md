Model predicts whether the entry is coachable or not
The Web Service takes csv file as input for upload
It returns new csv file with two additional columns { probabilities and coachable column }
coachable column have values 0 or 1
probabilites have [0,1)
